# 版本号：V2026.04.21.01

| 模块 | 详细内容 |
| --- | --- |
| 1. 新增功能 | 1. 订货管理：新增订货单列表/新增页、收货单、差异单、返货单、三方调拨单页面与路由入口；2. 采购管理：新增采购定价明细、采购单申请、采购单申请审核、采购订单、采购收货单、采购退货单页面与路由入口；3. 报表管理：新增采购订单状态跟踪表、退货单状态跟踪表、采购物品价格分析表、盘点盈亏表、出入库汇总表、其他出入库汇总表、机构间调拨明细/汇总表、批次全流程跟踪表、呆滞品查询表、库存周转率统计表、库存预警表；4. 库存查询：新增实时库存查询页，支持按机构、仓库、物品、统计类型等条件聚合展示。 |
| 2. 功能优化 | 1. 通用表格与固定面包屑交互优化，支撑新增单据/报表页面统一展示；2. 采购定价、调价、采购入库、菜品仓库关联等页面补齐供应商档案、物品规格、分类和库存字段联动；3. 流程发布历史管理支持双击展开和展开状态同步；4. 工作流待办查询支持门店作用域同时读取集团待办，并按审核时间和主键倒序排列。 |
| 3. BUG修复 | 1. 修复采购入库明细缺少规格、分类导致前端回显不完整的问题；2. 修复采购入库自动完成审批时重复生成待办通知的问题，仅在流程未完成时记录提交待办；3. 修正采购入库审核状态文案，由“复审”统一为“审核”；4. 修复流程发布历史表格展开状态与当前行不一致的问题。 |
| 4. 接口变更 | 1. `POST /api/inventory/purchase-inbound`、采购入库详情返回：采购入库明细行新增 `spec`、`category` 字段；2. `GET /api/inventory/purchase-inbound/balances`：新增实时库存查询前端调用类型定义；3. `GET /api/inventory/dish-consumption-outbound/report`、`GET /api/inventory/inout-detail/report`：新增菜品消耗出库报表和出入库明细表前端调用类型定义；4. 数据库脚本 `schema.sql`、`init.sql` 为 `inventory_purchase_inbound_line` 增加 `spec`、`category` 字段及幂等补列语句。 |

## 发布摘要

- 发布类型：功能迭代
- 计划发布时间：2026-04-21 19:30
- 分支名称：`main`
- 核心迭代内容：完成订货/采购单据、采购/库存报表和实时库存查询页面入口建设，补齐采购入库规格/分类字段，优化流程待办与发布历史交互，并同步数据库脚本。
- 验证结果：前端 `npm run build` 通过；后端 `mvn test` 通过，Checkstyle 0 违规。
- 备注：本次发布包含数据库字段变更，部署前需执行或确认 `inventory_purchase_inbound_line.spec/category` 补列；部分新增订货/采购单据页面仍以现有物品、供应商、仓库数据完成前端展示，完整业务接口需后续继续联调。
