<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boboboom.jxc.inventory.infrastructure.persistence.mapper.InventoryCheckMapper">

    <resultMap id="InventoryCheckHeaderMap" type="com.boboboom.jxc.inventory.application.service.InventoryCheckHeader">
        <id property="id" column="id"/>
        <result property="scopeType" column="scope_type"/>
        <result property="scopeId" column="scope_id"/>
        <result property="documentCode" column="document_code"/>
        <result property="checkDate" column="check_date"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="checkRangeType" column="check_range_type"/>
        <result property="freezeStock" column="freeze_stock"/>
        <result property="collaborativeFlag" column="collaborative_flag"/>
        <result property="planName" column="plan_name"/>
        <result property="thirdPartyDocument" column="third_party_document"/>
        <result property="salesmanUserId" column="salesman_user_id"/>
        <result property="salesmanName" column="salesman_name"/>
        <result property="itemCount" column="item_count"/>
        <result property="totalBookAmount" column="total_book_amount"/>
        <result property="totalActualAmount" column="total_actual_amount"/>
        <result property="totalDiffAmount" column="total_diff_amount"/>
        <result property="diffStatus" column="diff_status"/>
        <result property="generatedStatus" column="generated_status"/>
        <result property="printStatus" column="print_status"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="rejectionReason" column="rejection_reason"/>
        <result property="extraJson" column="extra_json"/>
        <result property="createdBy" column="created_by"/>
        <result property="approvedBy" column="approved_by"/>
        <result property="approvedAt" column="approved_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="InventoryCheckLineMap" type="com.boboboom.jxc.inventory.application.service.InventoryCheckLine">
        <id property="id" column="id"/>
        <result property="headerId" column="header_id"/>
        <result property="itemCode" column="item_code"/>
        <result property="itemName" column="item_name"/>
        <result property="spec" column="spec"/>
        <result property="category" column="category"/>
        <result property="unitName" column="unit_name"/>
        <result property="availableQty" column="available_qty"/>
        <result property="bookQty" column="book_qty"/>
        <result property="actualQty" column="actual_qty"/>
        <result property="bookPrice" column="book_price"/>
        <result property="bookAmount" column="book_amount"/>
        <result property="actualAmount" column="actual_amount"/>
        <result property="diffQty" column="diff_qty"/>
        <result property="diffAmount" column="diff_amount"/>
        <result property="profitQty" column="profit_qty"/>
        <result property="lossQty" column="loss_qty"/>
        <result property="profitLossReason" column="profit_loss_reason"/>
        <result property="profitInboundPrice" column="profit_inbound_price"/>
        <result property="profitAmount" column="profit_amount"/>
        <result property="lossOutboundPrice" column="loss_outbound_price"/>
        <result property="lossAmount" column="loss_amount"/>
        <result property="abnormalFlag" column="abnormal_flag"/>
        <result property="remark" column="remark"/>
        <result property="extraJson" column="extra_json"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <select id="selectHeadersByScopeOrdered" resultMap="InventoryCheckHeaderMap">
        select *
        from ${headerTable}
        where scope_type = #{scopeType}
          and scope_id = #{scopeId}
        order by created_at desc, id desc
    </select>

    <select id="selectHeadersByScopeAndIds" resultMap="InventoryCheckHeaderMap">
        select *
        from ${headerTable}
        where scope_type = #{scopeType}
          and scope_id = #{scopeId}
          and id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="!viewAll">
          and (created_by = #{createdBy} or salesman_user_id = #{createdBy})
        </if>
        order by id desc
    </select>

    <select id="selectHeaderByScopeAndId" resultMap="InventoryCheckHeaderMap">
        select *
        from ${headerTable}
        where scope_type = #{scopeType}
          and scope_id = #{scopeId}
          and id = #{id}
        <if test="!viewAll">
          and (created_by = #{createdBy} or salesman_user_id = #{createdBy})
        </if>
        order by id desc
    </select>

    <select id="countByScopeAndDocumentCode" resultType="java.lang.Long">
        select count(1)
        from ${headerTable}
        where scope_type = #{scopeType}
          and scope_id = #{scopeId}
          and document_code = #{documentCode}
    </select>

    <insert id="insertHeader" useGeneratedKeys="true" keyProperty="header.id">
        insert into ${headerTable}
        (scope_type, scope_id, document_code, check_date, warehouse_name, check_range_type, freeze_stock,
         collaborative_flag, plan_name, third_party_document, salesman_user_id, salesman_name, item_count,
         total_book_amount, total_actual_amount, total_diff_amount, diff_status, generated_status, print_status,
         status, remark, rejection_reason, extra_json, created_by, approved_by, approved_at, created_at, updated_at)
        values (#{header.scopeType}, #{header.scopeId}, #{header.documentCode}, #{header.checkDate},
                #{header.warehouseName}, #{header.checkRangeType}, #{header.freezeStock}, #{header.collaborativeFlag},
                #{header.planName}, #{header.thirdPartyDocument}, #{header.salesmanUserId}, #{header.salesmanName},
                #{header.itemCount}, #{header.totalBookAmount}, #{header.totalActualAmount}, #{header.totalDiffAmount},
                #{header.diffStatus}, #{header.generatedStatus}, #{header.printStatus}, #{header.status},
                #{header.remark}, #{header.rejectionReason}, #{header.extraJson}, #{header.createdBy},
                #{header.approvedBy}, #{header.approvedAt}, current_timestamp, current_timestamp)
    </insert>

    <update id="updateHeader">
        update ${headerTable}
        set check_date = #{header.checkDate},
            warehouse_name = #{header.warehouseName},
            check_range_type = #{header.checkRangeType},
            freeze_stock = #{header.freezeStock},
            collaborative_flag = #{header.collaborativeFlag},
            plan_name = #{header.planName},
            third_party_document = #{header.thirdPartyDocument},
            salesman_user_id = #{header.salesmanUserId},
            salesman_name = #{header.salesmanName},
            item_count = #{header.itemCount},
            total_book_amount = #{header.totalBookAmount},
            total_actual_amount = #{header.totalActualAmount},
            total_diff_amount = #{header.totalDiffAmount},
            diff_status = #{header.diffStatus},
            generated_status = #{header.generatedStatus},
            print_status = #{header.printStatus},
            status = #{header.status},
            remark = #{header.remark},
            rejection_reason = #{header.rejectionReason},
            extra_json = #{header.extraJson},
            approved_by = #{header.approvedBy},
            approved_at = #{header.approvedAt},
            updated_at = current_timestamp
        where id = #{header.id}
    </update>

    <delete id="deleteHeaderById">
        delete from ${headerTable}
        where id = #{id}
    </delete>

    <select id="selectLinesByHeaderIds" resultMap="InventoryCheckLineMap">
        select *
        from ${lineTable}
        where header_id in
        <foreach collection="headerIds" item="headerId" open="(" separator="," close=")">
            #{headerId}
        </foreach>
        order by id asc
    </select>

    <select id="selectLinesByHeaderId" resultMap="InventoryCheckLineMap">
        select *
        from ${lineTable}
        where header_id = #{headerId}
        order by id asc
    </select>

    <delete id="deleteLinesByHeaderId">
        delete from ${lineTable}
        where header_id = #{headerId}
    </delete>

    <insert id="insertLine" useGeneratedKeys="true" keyProperty="line.id">
        insert into ${lineTable}
        (header_id, item_code, item_name, spec, category, unit_name, available_qty, book_qty, actual_qty,
         book_price, book_amount, actual_amount, diff_qty, diff_amount, profit_qty, loss_qty, profit_loss_reason,
         profit_inbound_price, profit_amount, loss_outbound_price, loss_amount, abnormal_flag, remark, extra_json,
         created_at)
        values (#{line.headerId}, #{line.itemCode}, #{line.itemName}, #{line.spec}, #{line.category},
                #{line.unitName}, #{line.availableQty}, #{line.bookQty}, #{line.actualQty}, #{line.bookPrice},
                #{line.bookAmount}, #{line.actualAmount}, #{line.diffQty}, #{line.diffAmount}, #{line.profitQty},
                #{line.lossQty}, #{line.profitLossReason}, #{line.profitInboundPrice}, #{line.profitAmount},
                #{line.lossOutboundPrice}, #{line.lossAmount}, #{line.abnormalFlag}, #{line.remark},
                #{line.extraJson}, current_timestamp)
    </insert>
</mapper>
