<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boboboom.jxc.identity.infrastructure.persistence.mapper.StoreMapper">

    <resultMap id="StoreDOResultMap" type="com.boboboom.jxc.identity.infrastructure.persistence.dataobject.StoreDO">
        <id property="id" column="id"/>
        <result property="groupId" column="group_id"/>
        <result property="storeCode" column="store_code"/>
        <result property="storeName" column="store_name"/>
        <result property="status" column="status"/>
        <result property="contactName" column="contact_name"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="address" column="address"/>
        <result property="remark" column="remark"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <resultMap id="StoreAdminViewMap" type="com.boboboom.jxc.identity.infrastructure.persistence.query.StoreAdminView">
        <id property="storeId" column="store_id"/>
        <result property="storeCode" column="store_code"/>
        <result property="storeName" column="store_name"/>
        <result property="groupId" column="group_id"/>
        <result property="groupName" column="group_name"/>
        <result property="adminUserId" column="admin_user_id"/>
        <result property="adminRealName" column="admin_real_name"/>
        <result property="adminPhone" column="admin_phone"/>
        <result property="status" column="status"/>
    </resultMap>

    <select id="selectStoreAdminViewByGroupId" resultMap="StoreAdminViewMap">
        select s.id as store_id,
               s.store_code,
               s.store_name,
               s.group_id,
               g.group_name,
               sar.user_id as admin_user_id,
               u.real_name as admin_real_name,
               u.phone as admin_phone,
               s.status
        from sys_store s
                 inner join sys_group g on g.id = s.group_id
                 left join sys_store_admin_rel sar on sar.store_id = s.id and sar.status = 'ENABLED'
                 left join sys_user u on u.id = sar.user_id
        where s.group_id = #{groupId}
        <if test="status != null and status != ''">
            and s.status = #{status}
        </if>
        order by s.id desc
    </select>
</mapper>

